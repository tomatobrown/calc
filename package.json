{
    "name": "calc",
    "version": "1.0.0",
    "description": "A simple JavaScript project with unit tests",
    "scripts": {
        "test": "jest",
        "test:coverage": "jest --coverage --coverageReporters=json"
    },
    "devDependencies": {
        "jest": "^27.5.1"
    }
}